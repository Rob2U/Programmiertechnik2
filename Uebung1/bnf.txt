<zahl> ::= ("0" | ([1-9] ([0-9]+)?) ("," [0-9]+)?) | "pi" | "e" | ("M" [0-9])
<operator> ::= "\\" | "*" | "+" | "- " | "^" | "%"
<vollterm> ::= (<vollterm> ((<operator> <vollterm>)* | (<vollterm> "-> M" [0-9] (<operator> <vollterm>)?))) | <zahl> | ("(" <vollterm> ")") | ("round(" <vollterm> ")")


################################################################

<zahl> ::= ("0" | ([1-9] ([0-9]+)?) ("," [0-9]+)?) | "pi" | "e" | ("M" [0-9])
<operator> ::= "\\" | "*" | "+" | "- " | "^" | "%"

<round> ::= "round(" <exp> ")"

<klammern> ::= "(" <exp> ")"

<store> ::=  <exp> "->" "M" [0-9] (<operator> <exp>)?

<exp> ::= (<zahl> (<operator> <zahl>)*)? | <klammern> | <zahl> | <store>




################################################################
<zahl> ::= ("-")? ("0" | ([1-9] ([0-9]+)?) ("," [0-9]+)?) | "pi" | "e" | ("M" [0-9])
<operator> ::= "\\" | "*" | "+" | "- " | "^" | "%"

<round> ::= "round(" <exp> ")"

<klammern> ::= "(" <exp> ")"

<store> ::=  <exp> "->" "M" [0-9] (<operator> <exp>)?

<thing> ::= (<zahl> | <klammern> | <store>)

<exp> ::= (<thing> (<operator> <thing>)*)? | <klammern> | <zahl> | <store>

################################################### die untere sollte passen

<zahl> ::= ("-")? ("0" | ([1-9] ([0-9]+)?) ("," [0-9]+)?) | "pi" | "e" | ("M" [0-9])
<operator> ::= "/" | "*" | "+" | "-" | "^" | "%"

<round> ::= "round(" <exp> ")"

<klammern> ::= "(" <exp> ")"

<store> ::=  <exp> "->" "M" [0-9] (<operator> <exp>)?

<thing> ::= (<zahl> | <klammern> | <store> | <round>)

<exp> ::= (<thing> (<operator> <thing>)*)